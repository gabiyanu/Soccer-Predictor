# Firebase Cloud Functions runtime dependencies
# google-generativeai and gspread are intentionally omitted here —
# predictor.py imports them inside try/except and degrades gracefully.
# Add them back once the base deploy is confirmed working.

# Firebase
firebase-functions>=0.1.0
firebase-admin>=6.5.0,<7.0.0

# Web framework
flask>=2.3.0,<4.0.0
flask-cors>=4.0.0,<6.0.0

# Numerical / statistical models
numpy>=1.24.0,<3.0.0
scipy>=1.10.0,<2.0.0
