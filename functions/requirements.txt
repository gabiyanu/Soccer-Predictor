# Firebase Cloud Functions runtime dependencies
# These are installed automatically on `firebase deploy`

# Firebase
firebase-functions>=0.1.0
firebase-admin>=6.5.0

# Web framework
flask>=2.3.0
flask-cors>=4.0.0

# Numerical / statistical models
numpy>=1.24.0
scipy>=1.10.0

# Google Gemini AI  (optional — set GEMINI_API_KEY env var to enable)
google-generativeai>=0.7.0

# Google Sheets live data  (optional — set GOOGLE_SHEETS_ID + GOOGLE_SHEETS_JSON)
gspread>=6.0.0
google-auth>=2.28.0
