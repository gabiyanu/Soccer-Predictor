# Core web framework
flask>=2.3.0
flask-cors>=4.0.0
gunicorn>=21.0.0

# Numerical / statistical models
numpy>=1.24.0
scipy>=1.10.0

# Caching (prediction deduplication, 60-min TTL)
flask-caching>=2.1.0

# Google Gemini AI — AI-powered match narratives
# Requires: GEMINI_API_KEY environment variable
# Free tier: https://aistudio.google.com/app/apikey
google-generativeai>=0.7.0

# Google Sheets integration — live team data management
# Requires: GOOGLE_SHEETS_ID + GOOGLE_SHEETS_JSON (or GOOGLE_APPLICATION_CREDENTIALS)
gspread>=6.0.0
google-auth>=2.28.0
